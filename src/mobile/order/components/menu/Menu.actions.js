
const TONGLE_FULL_SCREAN_MODE = "TONGLE_FULL_SCREAN_MODE"

const MenuActions = {

	tongleFulScrenMode: function (){
		return{
			type:TONGLE_FULL_SCREAN_MODE,
		}
	}
}

export default MenuActions;
module.export = TONGLE_FULL_SCREAN_MODE;